#include <stdio.h>
int calculate(int a, int b, char c);
int calculate(int a, int b, char c){
    if(c=='+'){
        return a+b;
    }
    else if(c=='-'){
        return a-b;
    }
    else if(c=='*'){
        return a*b;
    }
    else{
        return a/b;
    }
}
int main() {
    int d = calculate(4, 5, '*');
    printf("%d", d);
    return 0;
}
