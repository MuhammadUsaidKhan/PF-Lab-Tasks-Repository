//do while loop is best



#include <stdio.h>
int main() {
    int num;
    int sum = 0;
    do{
        scanf("%d",&num);
        sum = sum+num;
        printf("%d\n",sum);
    }
    while(num!=0);
    return 0;
}
